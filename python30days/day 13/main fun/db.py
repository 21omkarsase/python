def connect_to_db():
    print("Connected with the database")

if __name__ == "__main__":
    connect_to_db()

    # In Python, the if __name__ == "__main__": construct is commonly used to define the main entry point of a script.
    # This code block is executed only when the
    # Python script is run directly and not when it's imported as a module in another script.
